#!/usr/bin/env bash
set -euo pipefail

script_dir="$(cd -- "$(dirname -- "$0")" && pwd)"
payload="$(cat)"
python3 "$script_dir/_internal/hook-pipeline" "TaskCancel" <<<"$payload" >/dev/null 2>&1 || true
echo '{}'