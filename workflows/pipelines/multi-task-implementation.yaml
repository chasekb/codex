id: multi-task-implementation
steps:
  - use: memory_load
  - use: inspect
  - use: backlog_load
  - use: plan
  - use: worktree_setup
  - use: parallel_dispatch
  - use: subagent_development
  - use: verify
  - use: review_request
  - use: branch_finish
  - use: backlog_sync
  - use: report
  - use: memory_commit
budget_profile: implement
output_contract: request_standard
