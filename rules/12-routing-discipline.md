# Routing Discipline

- Select one primary tool for each step.
- Use fallback tools only when primary fails or is insufficient.
- Do not fan out across multiple broad-search tools by default.
- For dependent steps, execute sequentially and re-evaluate after each result.
