# Reuse Over Repeat

- Centralize templates in `MCP/prompts/*`.
- Centralize workflow primitives in `workflows/shared/*`.
- Reference existing rule clauses rather than restating them.
- When adding new guidance, update canonical source first, then link.
