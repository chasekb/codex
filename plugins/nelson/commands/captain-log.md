# Nelson Captain's Log

Use this command when you need the current Nelson mission state, recent coordination history, or the current action state without reading the validation files directly.

It is the first-class captain's log surface for the package. The surface should expose the latest action state plus recent log entries so mission progress can be inspected as a live package capability.
