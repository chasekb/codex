# Nelson Package Context

Use this command when you need the current Nelson package shape, surface area, or session-ready context.

It summarizes the package manifest, command registry, skills, rules, workflows, validation coverage, and the public Nelson vocabulary aliases for sailing orders, battle plans, action stations, and captain's log so routing and session startup can see the Nelson boundary clearly.
It also reflects the mission lifecycle workflow and the captain's log surface so package context stays aligned with the remaining parity gaps.
