# Nelson Package Context

Use this command when you need the current Nelson package shape, surface area, or session-ready context.

It summarizes the package manifest, command registry, skills, rules, workflows, and validation coverage so routing and session startup can see the Nelson boundary clearly.
