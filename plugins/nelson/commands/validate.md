# Nelson Package Validation

Use this command when you need to verify that the Nelson package is internally consistent and runnable.

It covers package validation, mission scenarios, the command/routing surface, and the public vocabulary alias coverage needed to prove the package is ready for use.
