version: 1
intents:
  plan:
    primary: local_files
    fallback: none
    workflow: plan-only
    min_confidence: 0.55
    pre_steps: [skill_select, skill_load, memory_load]
    post_steps: [memory_commit]
  request_change:
    primary: local_files
    fallback: none
    workflow: request-standard
    min_confidence: 0.65
    pre_steps: [skill_select, skill_load, memory_load]
    post_steps: [memory_commit]
  implementation_execution:
    primary: local_files
    fallback: none
    workflow: implementation-execution
    min_confidence: 0.70
    pre_steps: [skill_select, skill_load, memory_load]
    post_steps: [memory_commit]
  ci_triage:
    primary: local_files
    fallback: none
    workflow: ci-triage
    min_confidence: 0.65
    pre_steps: [skill_select, skill_load, memory_load]
    post_steps: [memory_commit]
  release_publish:
    primary: local_files
    fallback: none
    workflow: release-publish
    min_confidence: 0.75
    pre_steps: [skill_select, skill_load, memory_load]
    post_steps: [memory_commit]
  dependency_maintenance:
    primary: local_files
    fallback: none
    workflow: dependency-maintenance
    min_confidence: 0.65
    pre_steps: [skill_select, skill_load, memory_load]
    post_steps: [memory_commit]
  docs_lookup:
    primary: context7
    fallback: brave
    workflow: research
    min_confidence: 0.65
    pre_steps: [skill_select, skill_load, memory_load]
    post_steps: [memory_commit]
  bug_debug:
    primary: local_files
    fallback: playwright
    workflow: debug-rca
    min_confidence: 0.70
    pre_steps: [skill_select, skill_load, memory_load]
    post_steps: [memory_commit]
  debug_deep_dive:
    primary: local_files
    fallback: playwright
    workflow: debug-deep-dive
    min_confidence: 0.70
    pre_steps: [skill_select, skill_load, memory_load]
    post_steps: [memory_commit]
  ui_validation:
    primary: playwright
    fallback: local_files
    workflow: verify
    min_confidence: 0.70
    pre_steps: [skill_select, skill_load, memory_load]
    post_steps: [memory_commit]
  external_research:
    primary: brave
    fallback: context7
    workflow: research
    min_confidence: 0.60
    pre_steps: [skill_select, skill_load, memory_load]
    post_steps: [memory_commit]
  skill_maintenance:
    primary: local_files
    fallback: none
    workflow: skill-maintenance
    min_confidence: 0.55
    pre_steps: [skill_select, skill_load, memory_load]
    post_steps: [memory_commit]
  project_backlog:
    primary: local_files
    fallback: none
    workflow: project-backlog-management
    min_confidence: 0.55
    pre_steps: [skill_select, skill_load, memory_load]
    post_steps: [memory_commit]
  weekly_backlog_review:
    primary: local_files
    fallback: none
    workflow: weekly-backlog-review
    min_confidence: 0.55
    pre_steps: [skill_select, skill_load, memory_load]
    post_steps: [memory_commit]
  backlog_triage:
    primary: local_files
    fallback: none
    workflow: backlog-triage
    min_confidence: 0.55
    pre_steps: [skill_select, skill_load, memory_load]
    post_steps: [memory_commit]
  recommendation_to_task:
    primary: local_files
    fallback: none
    workflow: recommendation-to-task
    min_confidence: 0.55
    pre_steps: [skill_select, skill_load, memory_load]
    post_steps: [memory_commit]
  review_closeout:
    primary: local_files
    fallback: none
    workflow: review-closeout
    min_confidence: 0.65
    pre_steps: [skill_select, skill_load, memory_load]
    post_steps: [memory_commit]
  implementation:
    primary: local_files
    fallback: none
    workflow: implement-small
    min_confidence: 0.70
    pre_steps: [skill_select, skill_load, memory_load]
    post_steps: [memory_commit]
  multi_task_implementation:
    primary: local_files
    fallback: none
    workflow: multi-task-implementation
    min_confidence: 0.70
    pre_steps: [skill_select, skill_load, memory_load]
    post_steps: [memory_commit]
  retrospective:
    primary: local_files
    fallback: none
    workflow: retro
    min_confidence: 0.80
    pre_steps: [skill_select, skill_load, memory_load]
    post_steps: [memory_commit]
rules:
  - pick_one_primary_tool_first
  - fallback_only_if_primary_fails_or_insufficient
  - never_parallelize_dependent_steps
  - low_confidence_routes_to_plan_only
